---
title: "Fonts"
description: "搜索 Figma 中可用的字体。"
---

import ParameterTable from "../../../../components/ParameterTable.astro";

搜索 Figma 中可用的字体。

<details>
<summary>Type reference</summary>

```ts
// Returns font family names installed in the Figma environment. Use to verify a fontFamily before text.create or styles.create.
// query filters by family name substring (case-insensitive), e.g. query:"inter" matches "Inter", "Inter Tight".
// ---
// Default response: just family names. Set includeStyles:true to also get available styles per family (e.g. "Regular", "Bold", "Italic").
// Use fonts.list with query to narrow results before creating text with specific fontFamily + fontStyle.
```

</details>

1 method available.

## list

List available font families, optionally filtered by name

<ParameterTable schema={{"type":"object","properties":{"query":{"description":"Filter by family name (case-insensitive substring)","type":"string"},"includeStyles":{"description":"Include available styles per family (default: false)","type":"boolean"},"offset":{"description":"Skip N items for pagination (default 0) (default: 0)","type":"number"},"limit":{"description":"Max items per page (default 100) (default: 100)","type":"number"}}}} />

**Response**

<ParameterTable schema={{"type":"object","properties":{"count":{"description":"Number of matching families","type":"number"},"fonts":{"description":"Array of {family} stubs, or {family, styles[]} when includeStyles:true","type":"array","items":{"type":"object","properties":{"family":{"type":"string"},"styles":{"type":"array","items":{"type":"string"}}},"required":["family"]}}},"required":["count"]}} fieldLabel="Field" hideRequired />
