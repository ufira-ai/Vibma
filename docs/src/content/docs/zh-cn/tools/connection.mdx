---
title: "Connection"
description: "管理 Figma 插件连接。"
---

import ParameterTable from "../../../../components/ParameterTable.astro";

管理 Figma 插件连接。

<details>
<summary>Type reference</summary>

```ts
// Connection manages the WebSocket link between the MCP server and the Figma plugin.
// Channels are named rooms — both the MCP server and Figma plugin must join the same channel to communicate.
// Workflow: connection(method:"create") to join a channel → connection(method:"get") to verify Figma plugin is connected.
// If get times out (5s), the Figma plugin is not running or not on the same channel.
// list shows all active channels and their connected clients. delete factory-resets a channel.
```

</details>

4 methods available.

## create

Join a relay channel (required first step before any other tool)

<ParameterTable schema={{"type":"object","properties":{"channel":{"description":"The channel name displayed in the Figma plugin panel. Defaults to 'vibma' if omitted. (default: \"vibma\")","type":"string"}}}} />

**Response**

Confirmation message with channel and port

<ParameterTable schema={{"type":"object","properties":{"result":{"type":"string","description":"Confirmation message with channel and port"}}}} fieldLabel="Field" hideRequired />

<details>
<summary>Example response</summary>

```json
"Joined channel \"vibma\" on port 3055. Call connection(method: \"get\") to verify the Figma plugin is connected."
```

</details>

## get

Verify end-to-end connection to Figma

No parameters.

**Response**

<ParameterTable schema={{"type":"object","properties":{"status":{"const":"pong"},"documentName":{"description":"Active Figma document name","type":"string"},"currentPage":{"description":"Current page name","type":"string"},"timestamp":{"description":"Unix timestamp","type":"number"}}}} fieldLabel="Field" hideRequired />

<details>
<summary>Example response</summary>

```json
{
  "status": "pong",
  "documentName": "Design System",
  "currentPage": "Components",
  "timestamp": 1709337600
}
```

</details>

## list

Inspect which clients (MCP, plugin) are connected to each channel

No parameters.

## delete

**edit**

Disconnect all clients (MCP server and Figma plugin) from a channel and reset its state

<ParameterTable schema={{"type":"object","properties":{"channel":{"description":"Channel to reset. Defaults to 'vibma'. (default: \"vibma\")","type":"string"}}}} />

**Response**

Confirmation of tunnel reset

<ParameterTable schema={{"type":"object","properties":{"result":{"type":"string","description":"Confirmation of tunnel reset"}}}} fieldLabel="Field" hideRequired />

<details>
<summary>Example response</summary>

```json
"Channel vibma reset"
```

</details>
