{
  "name": "vibma",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "node dist/mcp.js",
    "socket": "tsx packages/tunnel/src/index.ts",
    "setup": "./scripts/setup.sh",
    "build": "npm run build -w packages/core && npm run build -w packages/adapter-figma",
    "build:watch": "npm run build:watch -w packages/core & npm run build:watch -w packages/adapter-figma",
    "dev": "npm run build:watch",
    "dev:mcp": "tsx packages/core/src/mcp.ts",
    "pub:release": "npm run build && npm publish -w packages/core && npm publish -w packages/adapter-figma",
    "docs:generate": "tsx scripts/extract-tools.ts",
    "docs:dev": "npm run docs:generate && cd docs && npm run dev",
    "docs:build": "npm run docs:generate && cd docs && npm run build"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
